# demo-repo
Testing the Github
scorching hat
find me the new call

## Streamlit CSV Analyzer

This repository now includes a Streamlit app for uploading CSV files, inspecting data, generating basic charts, and downloading a cleaned dataset.

### Run locally

```bash
pip install -r requirements.txt
streamlit run streamlit_app.py
```
